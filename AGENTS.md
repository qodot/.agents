사용자에게 보고하고 끝내기 전에, 자율 행동이 가능한지 생각해보고 명백히 다음 행동이 예상되는 경우(추측 금지) 즉시 실행.

진행 중인 작업에서 사용자가 오류·누락을 지적하거나 전제를 정정하면, 해명만 하고 멈추지 않는다. 정정을 기존 목표의 실행 입력으로 반영하고, 이미 승인된 범위 안에 남은 후속 행동이 있으면 즉시 수행한 뒤 결과를 보고한다. 사용자가 이유만 물은 턴에는 이유만 답하되, 이후 미실행을 지적하거나 행동을 요구하면 같은 설명을 반복하지 말고 바로 실행한다.

## 최소 변경과 기존 구현 재사용

- 구현 전에 실제 호출처와 기존 수집·저장·조회 경로를 확인한다. 기존 코드로 가능한 기능을 별도 서비스·태스크·엔드포인트·래퍼로 중복 구현하지 않는다.
- 현재 요구를 만족하는 최소 변경만 한다. 필수 근거가 없는 스키마 필드·테이블·상태 추적·카운터·진행률 보고·완료 알림·웹훅을 추가하지 않는다. 기존 저장 결과로 계산할 수 있으면 조회 시 계산한다.
- 수집 범위와 상한은 사용자가 정한 기준을 따른다. 단순한 표본 수집이나 대략적인 진행률 조회를 정밀한 작업 추적 체계로 확대하지 않는다.
- 실제로 사용되는 호출 계약을 기준으로 함수 입력을 설계한다. 확인되지 않은 과거 호출 방식이나 가상의 호환성을 위해 선택 인자·분기를 남기지 않는다. 전달된 레코드에서 알 수 있는 값은 중복 인자로 받지 않는다.
- 예외 처리가 필요한 태스크는 비즈니스 로직을 비공개 함수에 두고, 태스크 진입점에서 그 호출을 try/except로 감싸 실패 처리와 예외 전파를 담당하게 한다. 이를 위해 별도 실행 태스크나 서비스 계층을 추가하지 않는다.

## 지적 반영과 완료 보고

- 진행 중인 작업의 잘못된 구현이나 불필요한 선택을 지적받았고 수정 방향이 명확하면, 사과·해명·동의만 하고 턴을 끝내지 않는다. 별도의 "진행해"를 기다리지 말고 이미 승인된 범위에서 실제 코드 수정과 필요한 검증까지 수행한 뒤 결과를 보고한다.
- "수정하겠다", "제거하는 게 맞다", "기존 방식으로 맞추겠다"라고 말했으면 해당 작업을 실제로 실행한다. 실행할 수 없다면 구체적인 차단 원인과 미완료 범위를 밝힌다. 이유만 묻는 질문과 현재 작업의 수정을 요구하는 지적을 문맥으로 구분하고, 후자를 설명 요청으로 축소하지 않는다.
- 불필요한 구조를 지적받으면 해당 파일만 고치지 말고 현재 변경 전체에서 같은 문제가 반복되는지 점검한다. 이미 승인된 범위에서 중복 구조와 그 의존 코드를 함께 정리한다.
- 제거 요청은 실제 코드와 호출 경로를 제거하라는 뜻이다. 이름만 바꾸거나 다른 계층으로 옮겨 남겨두고 제거했다고 보고하지 않는다.
- 설명·제안과 실제 실행을 구분한다. 사용자가 실행을 지시하면 같은 설명을 반복하며 끝내지 말고 수정, 필요한 검증, 이미 승인된 커밋·푸시·PR 반영까지 수행한다.
- 확인하지 않은 기존 경로나 요구사항을 사실처럼 설명하지 않는다. 완료 보고는 실제 diff와 실행 결과를 근거로 하며, 남은 제한이나 미완료 사항을 숨기지 않는다.

## 커밋과 문서

- 커밋은 하나의 기능이나 책임 단위로 작게 나누고 관련 테스트를 함께 묶는다. 서로 다른 계층의 변경을 큰 커밋 하나에 몰아넣지 않는다.
- 기존 PR의 후속 수정은 수정 커밋을 계속 덧붙이지 말고 해당 변경을 도입한 커밋에 fixup/autosquash한다. 여러 책임에 걸친 수정은 적절한 원래 커밋에 나누어 반영한다.
- 히스토리 정리 후 최종 코드가 의도대로 유지되는지 확인한다. 푸시가 승인된 범위에서는 원격 HEAD를 확인하고 force-with-lease로 갱신한 뒤 실제 PR HEAD를 검증한다.
- PR 설명은 최종 문제·변경·검증 중심으로 짧게 쓴다. 사용자 요청이나 명시적인 저장소 규칙 없이 별도 설명 문서를 새로 만들지 않는다.
